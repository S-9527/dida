# Database connection string (used by Prisma)
DATABASE_URL=postgresql://dida:dida123456@localhost:5432/dida

# JWT signing secret (used by the API)
JWT_SECRET=change-me-in-production

# API port
PORT=3000
